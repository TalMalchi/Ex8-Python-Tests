# import json
# import time
# from unittest import TestCase
#
# from networkx import *
#
# from src.Agent import Agent
# from src.Point import Point
# from src.Pokemon import Pokemon
#
# class test_Algorithms(TestCase):


    #def test_assign_agent_src_nodes(self):
        # graph = nx.DiGraph()
        # pokLst = json.loads('{"Pokemons":[{"Pokemon":{"value":5.0,"type":-1,"pos":"35.197656770719604,32.10191878639921,0.0"}}]}')['Pokemons'][0]
        # pok.parsePokemon(pokLst)
        # s1 = json.loads('{"Agents":[{"Agent":{"id":0,"value":0.0,"src":0,"dest":1,"speed":1.0,"pos":"35.18753053591606,32.10378225882353,0.0"}}]}')['Agents'][0]['Agent']
        # ag.parseAgent(s1)
        # PORT = 6666
        # HOST = '127.0.0.1'
        # client = Client()  # init of Client
        # client.start_connection(HOST, PORT)  # init connection to server
        # graph = loadGraph(client.get_graph())  # load the graph (as instance of networkx graph)
        # pokLst = loadAllPokemons(client.get_pokemons(), graph)  # load Pokemon list
        # caseInfo = json.loads(client.get_info())
        # numOfAgents = caseInfo['GameServer']['agents']
        # numOfAssignedAgentsToPok = assignAgentSrcNodes(numOfAgents, client, pokLst, graph)
        # self.assertEqual(numOfAssignedAgentsToPok, 1)


    #def sortPokLst(self):

    #def assignAgentSrcNodes(self):

#checkkk